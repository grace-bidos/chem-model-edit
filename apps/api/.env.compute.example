# User-managed compute node (AiiDA + Slurm)
#
# Register the node from Web SaaS generated command:
#   curl -fsSL <api>/api/runtime/nodes/install.sh | bash -s -- --api-base <api> --join-token <token>
#
# This file is intentionally Redis-free.

# Runtime API base (management node FastAPI)
RUNTIME_API_BASE=http://localhost:8000

# Optional default queue hint for local scripts
RUNTIME_QUEUE_NAME=standard
